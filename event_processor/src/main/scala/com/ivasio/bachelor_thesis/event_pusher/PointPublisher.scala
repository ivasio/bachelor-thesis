package com.ivasio.bachelor_thesis.event_pusher

import scala.util.Random


class PointPublisher {
  val xRange: (Float, Float) = Tuple2[Float, Float](1, 5)

  def main(): Unit = {
    Random.nextFloat()
  }
}
